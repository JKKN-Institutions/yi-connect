# WhatsApp session folders (local dev only, not for serverless)
.wwebjs_auth/
.wwebjs_cache/

# whatsapp-service (separate Railway deployment)
whatsapp-service/

# Other dev files
.env*
*.log

# Heavy local-only dirs (Vercel installs deps + builds fresh; these must not upload)
.claude/
node_modules/
.next/
.git/
backups/
